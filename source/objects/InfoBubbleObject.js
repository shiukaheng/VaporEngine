var ThreeMeshUI = require("three-mesh-ui")

/**
 * Parameters:
 * - Description text
 * - Photo (Optional)
 * - Layout: Horizontal or Vertical
 * - Image height (If unset, uses text height)
 * - Image width (If left unset, calculates from image aspect with height)
 * - Image would always fit to the dimensions (?)
 */